package process;

import model.Exchange;
import model.Money;

public class Exchanger {
    
    public Exchange exchange(Money.getAmount(), ExchangeRate.getValue()){
        return new Money;
    }
}
